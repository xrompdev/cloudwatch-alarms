{
  "name": "alarm-triager",
  "version": "1.0.0",
  "description": "Intelligent alarm triage with Playwright, Datadog, and Slack",
  "main": "index.js",
  "scripts": {
    "build": "npm install --production && cd .. && ./scripts/build-triager.sh"
  },
  "dependencies": {
    "@aws-sdk/client-cloudwatch": "^3.400.0",
    "@aws-sdk/client-s3": "^3.400.0",
    "axios": "^1.6.0",
    "playwright-core": "^1.40.0",
    "playwright-aws-lambda": "^0.10.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
